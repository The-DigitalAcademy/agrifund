export const environment = {
    production: false,
    // Change proxy file target to "target": "https://agrifund.tech to use deployed api and not local host",
    API_URL: '/api/v2', //completes the proxy url for the api connection
    // MOCK_URL: 'http://localhost:3001/agrifund/api/v1',
};
